CREATE DATABASE mjv_marketplace;
USE mjv_marketplace;
CREATE TABLE `stores`(
	`Id` INT(10) AUTO_INCREMENT PRIMARY KEY,
	`FantasyName` VARCHAR(50) NULL DEFAULT NULL,	
	`Cnpj` VARCHAR(50) NULL DEFAULT NULL, 
	`CorporateName` VARCHAR(50) NULL DEFAULT NULL,
	`Address` VARCHAR(50) NULL DEFAULT NULL,
	`Phone` VARCHAR(50) NULL DEFAULT NULL,
	`Responsible` VARCHAR(50) NULL DEFAULT NULL
)ENGINE=InnoDB;

CREATE TABLE `products`(
	`Id` INT(10) AUTO_INCREMENT PRIMARY KEY,
	`Name` VARCHAR(50) NULL DEFAULT NULL,	
	`Description` VARCHAR(100) NULL DEFAULT NULL, 
	`Category` VARCHAR(50) NULL DEFAULT NULL, 
	`Image` VARCHAR(255) NULL DEFAULT NULL,
	`Price` INT(20) NULL DEFAULT NULL,
	`Quantity` INT(20) NULL DEFAULT NULL,
	`store_id` INT(10) NULL DEFAULT NULL
)ENGINE=INNODB;


CREATE TABLE `users`(
	`ID` INT(10) NOT NULL AUTO_INCREMENT,
	`Login` VARCHAR(50) UNIQUE NOT NULL,
	`AccessKey` VARCHAR(50) NOT NULL,
	PRIMARY KEY(`ID`)
)
ENGINE=InnoDB





REQUISITOS:
	CRUD LOJAS:
		NOME FANTASIA;
		CNPJ;
		RAZÃO SOCIAL;
		ENDEREÇO;
		TELEFONE;
		RESPONSÁVEL;

	CRUD PRODUTOS:	
		NOME PRODUTO;
		DESCRIÇÃO PRODUTO;
		CATEGORIA;
		IMAGEM;
		PREÇO PRODUTO;
		STORE ID;

	PESQUISA A PARTIR DE UMA PALAVRA QUE TENHA COMO RESULTADO OS PRODUTOS DISPONÍVEIS EM TODAS AS LOJAS COM SEUS RESPECTIVOS VALORES;

TODOS:
	PARA FUNCIONAMENTO DO QUE JÁ EXISTE:
	
	FUNÇÕES:
		FindByProdName
		Reverter mudança feita no nome das entidades
		mudar o findbyterm para retornar varios

	DETALHES:
		--> Disponibilidade em estoque;
		--> Avaliação da loja;
		--> Produtos Relacionados;

FIRULAS:
	Testes automatizados;
	Testes Unitarios;
	Autenticação;
	JWT;
	Annotations;
	Design Pattern;
	Live Demo.



SAVE HEIDI:

USE mjv_marketplace;
CREATE TABLE `stores`(
	`Id` int(10) UNSIGNED NULL DEFAULT NULL,
	`FantasyName` VARCHAR(50) NULL DEFAULT NULL,	
	`Cnpj` VARCHAR(50) NULL DEFAULT NULL, 
	`CorporateName` VARCHAR(50) NULL DEFAULT NULL,
	`Address` VARCHAR(50) NULL DEFAULT NULL,
	`Phone` VARCHAR(50) NULL DEFAULT NULL,
	`Responsible` VARCHAR(50) NULL DEFAULT NULL,
)ENGINE=INNODB;



CREATE TABLE `users`(
	`ID` INT(10) NOT NULL AUTO_INCREMENT,
	`Login` VARCHAR(50) UNIQUE NOT NULL,
	`AccessKey` VARCHAR(50) NOT NULL,
	PRIMARY KEY(`ID`)
)
ENGINE=INNODB

ALTER TABLE stores CHANGE Id Id INT(10) AUTO_INCREMENT PRIMARY KEY;